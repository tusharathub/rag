# RAG Backend

Production-grade RAG (Retrieval-Augmented Generation) Backend following Clean Architecture.

## Setup

1. Make sure you have `uv` installed.
2. Run `uv sync` to install dependencies and set up the virtual environment.
3. Configure the environment variables.
4. Run the development server with `uv run uvicorn app.main:app --reload`.
